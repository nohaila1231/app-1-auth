name: CI - Auth Service

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: nohaila1231/template-pro/.github/workflows/ci.yml@main
    with:
      node-version: '18'
      run-tests: true
